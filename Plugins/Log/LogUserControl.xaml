<local:HzUserControl x:Class="HZBot.LogUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HZBot"
                     d:DataContext="{d:DesignInstance local:HzAccount}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            FontSize="18"
            FontWeight="Bold"
            Text="Log:" Grid.Row="0" />
        <ListView Grid.Row="1" ItemsSource="{Binding Log.LogList}"/>
        <!--<ScrollViewer Grid.Row="1">
            <TextBlock Text="{Binding Log.GetLogsAsString}"/>
        </ScrollViewer>-->
    </Grid>
</local:HzUserControl>